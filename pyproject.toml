[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-redteam-core"
version = "0.1.0"
description = "CLI-first baseline for adversarial flow experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
